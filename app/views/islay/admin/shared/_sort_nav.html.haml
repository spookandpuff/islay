%ul{:class => "sort fold-true"}
  %li.sort-label Sort by
  - entries.each do |name, url, current|
    %li
      - if current
        = link_to(name, url, :class => 'current')
      - else
        = link_to(name, url)
