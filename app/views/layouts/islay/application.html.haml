!!!
%html
  %head
    %title
      Islay
      - if _header
        \- #{_header}
    %meta(http-equiv='Content-Type' content='text/html')
    %meta(content="text/html;charset=UTF-8" http-equiv="Content-Type")

    = stylesheet_link_tag('islay/admin/islay')
    = stylesheet_link_tag('islay/admin/islay_print', :media => 'print')
    = stylesheet_link_tag("//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css")
    = javascript_include_tag('islay/admin/islay')

  %body(id=body_id class=body_class)
    %h1.islay-layout-app-name 
      Islay

    %div.islay-layout-nav
      %ul.islay-main-nav{:class => _nav ? 'closed' : 'open'}
        %li
          = form_tag(path(:search), :method => :get, :class => 'islay-global-search') do
            %i.icon-search
            = text_field_tag('term', nil, :placeholder => 'Search')

        = main_nav("Dashboard", 'home', :dashboard, :root => true)
        = main_nav("Reports", 'bar-chart', :reports)
        = extension_nav_entries 
        = main_nav("Page Contents", 'file-text', :pages)
        = main_nav("Assets", 'picture', :assets)
        = main_nav("Configuration", 'cog', :users)

        %li.islay-current-user
          = form_for(current_user, :url => :destroy_user_session, :method => :delete, :html => {:class => 'entry'}) do |f|
            %i.icon-user
            %strong= current_user.name
            %button(type='submit') 
              %i.icon-signout
              %span Sign out

      - if _nav
        - render :partial => _nav
        %ul.islay-sub-nav
          - sub_nav_entries.each do |entry|
            %li= entry

    %div.islay-layout-header
      %h2= _header
      = render_breadcrumbs
      = render_statuses
      = render_filter_nav
      = render_sort_nav

    = yield
